export default function SupdataLogo({ className = "" }) {
    return (
        <img
            src="/supdata-logo.png"
            alt="SUPDATA logo"
            className={`h-10 w-auto ${className}`}
        />
    );
}
